// constructors
fn Str
    ret Vec[char]()

fn Str size: i32 c: char
    ret Vec[char](size c)

fn _Str_from_raw str: _string
    res = Str()
    i = 0
    while _builtin_index_str(str i) != char 0
        res.push(_builtin_index_str(str i))
        i++
    ret res

