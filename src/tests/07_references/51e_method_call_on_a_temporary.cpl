//ERR 14 12 14 19
//MSG="No candidate found for this function call"

struct TestStruct
    x

    fn set_x
        self.x = "compile"

    fn get_x
        ret self.x

fn main
    a = (TestStruct x "no good").set_x()
    out "{a.get_x()}"
