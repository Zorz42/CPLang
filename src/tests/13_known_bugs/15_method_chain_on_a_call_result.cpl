//BUG="FEEDBACK.md 1.10 — postfix operators do not apply to a call result"
//OUT="3"

// Same cause as 13, for a method call on the result of an ordinary function.

fn three
    v = Vec()
    v.push(1)
    v.push(2)
    v.push(3)
    ret v

fn main
    out "{three().size()}"
