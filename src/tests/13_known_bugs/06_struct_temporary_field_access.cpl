//BUG="FEEDBACK.md 1.4 — codegen panics on a struct or tuple temporary"
//OUT="7"

// Same panic as 04, with a named struct rather than a tuple.

struct Point
    x: i32
    y: i32

fn main
    out "{(Point x 7 y 8).x}"
