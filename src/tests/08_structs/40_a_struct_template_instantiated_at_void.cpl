//OUT="3"

// `void` reaching a field through a template argument rather than through the
// values written in the instantiation.

fn nothing
    ret

struct Box[T]
    v: T
    n: i32

fn main
    b = Box[void] v nothing() n 3
    out "{b.n}"
