//OUT="1 4 9"

// Newlines inside a brace block are whitespace, so a function generated by a
// macro needs explicit braces around its body.

macro define_square name {
    fn name x {
        ret x * x
    }
}

define_square sq;

fn main
    out "{sq(1)} {sq(2)} {sq(3)}"
