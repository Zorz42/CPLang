//OUT=10 ok

struct Box[T]
    x: T

struct Tuple[A B]
    first: Box[A]
    second: Box[B]

make_tuple[A B] a: A b: B
    ret Tuple[A B] first a second b

main
    a = make_tuple(Box x 10 Box x "ok")
    out "{a.first.x} {a.second.x}"