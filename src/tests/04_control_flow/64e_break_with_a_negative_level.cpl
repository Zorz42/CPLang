//ERR 7 8 7 11

// The same as 63e_break_level_of_zero.cpl below zero. Whether the level is
// parsed as a literal or as an expression, a negative one has no meaning.

fn main
    for i 0..3
        brk -1
