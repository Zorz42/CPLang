//OUT="00 02 10 12 "

// 39_continue_affects_only_the_innermost_loop.cpl with the level spelled out.
// `cnt 1` and a bare `cnt` are the same statement.

fn main
    for i 0..2
        for j 0..3
            if j == 1
                cnt 1
            out "{i}{j} "
