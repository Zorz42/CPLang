//ERR 9 8 9 11
//MSG="Maximum possible depth for this break statement is 0."

// An `if` is a block but not a loop, so nesting the `brk` inside one does not
// give it a target. A check that looks for "am I inside a block" rather than
// "am I inside a loop" would accept this.

fn main
    if true
        brk
