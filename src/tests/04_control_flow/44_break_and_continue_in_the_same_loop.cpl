//OUT="1 3 5 "

// The two keywords in one `while true`: `cnt` filters and `brk` terminates.
// This is the shape most search loops take.

fn main
    i = 0
    while true
        i++
        if i > 6
            brk
        if i % 2 == 0
            cnt
        out "{i} "
