//OUT=10

struct Vec
    size
    vec

    fn index i
        ret &_builtin_index(self.vec i)

new_vec size
    ret Vec size size vec _builtin_alloc(size)

new_vec
    ret new_vec(0)

main
    vec = new_vec(10)
    :vec.index(0) = 10
    out "{:vec.index(0)}"