//OUT="1 1 2"

// Both parameters share one template type and both calls pass the same
// variable, so the resolver is asked to unify a type label with itself.

fn pair[T] a: T b: T
    ret a + b

fn main
    x = 1
    out "{x} {x} {pair(x x)}"
